name: CI

on: [push]

jobs:
  build:
    name: Build
    runs-on: windows-2019
    steps:
      - name: Checkout branch
        uses: actions/checkout@master
      - name: Setup
        run: quickbuild.bat --i-agree-to-the-vendor-licenses -j 2 address-model=64 msconvert
